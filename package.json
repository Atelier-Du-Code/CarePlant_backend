{
  "name": "CarePlant",
  "private": true,
  "scripts": {
    "build": "npm run buildBackend",   
    "buildBackend": "cd backend && npm install && npm run build",
    "start": "npm run startBackend",  
    "startBackend": "cd backend && npm run server",
    "dev": "concurrently \"npm run startBackend\"",
    "debug": "npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "cors": "^2.8.5"
  }
}
